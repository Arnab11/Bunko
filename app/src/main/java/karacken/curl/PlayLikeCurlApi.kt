package karacken.curl

/** Version marker for the public bitmap page-deck integration contract. */
object PlayLikeCurlApi {
    /**
     * Numeric production API version.
     *
     * Consumers should pin both this value and the repository revision they embed.
     */
    const val PRODUCTION_API_VERSION: Int = 1
}
